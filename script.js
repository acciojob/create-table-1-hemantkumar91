function insert_Row() {
    //Write your code here
  
  Here’s the `script.js` file with the `insert_Row()` function implemented:

This function retrieves the table by its ID, inserts a new row at the top (index 0), adds two new cells to the row, and assigns the required text values to each cell. Let me know if you need any modifications! 🚀
}
